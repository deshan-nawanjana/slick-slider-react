/** Outer element default style */
export const outerCSS: React.CSSProperties = {
  overflow: 'hidden',
  userSelect: 'none',
  touchAction: 'pan-y'
}

/** Inner element default style */
export const innerCSS: React.CSSProperties = {
  display: 'flex',
  gap: 'inherit',
  width: 'fit-content'
}

/** Child element default style */
export const childCSS: React.CSSProperties = {
  flexShrink: 0,
  display: 'flex'
}
